# Core RL Framework
stable-baselines3==2.7.0
gymnasium==1.0.0
shimmy[gym-v26]==2.0.0

# PyTorch for Apple Silicon (install first, separate from others)
torch==2.5.1
torchvision==0.20.1

# Install these system packages with homebrew BEFORE pip install:
# brew install cmake swig pkg-config

# Box2D environments (BipedalWalker, LunarLander)
# Try pre-built wheel first, fallback to source
gymnasium[box2d]==1.0.0

# PyBullet environments (Ant, HalfCheetah, Hopper, Walker2D)
# Use specific version known to work on M-series
pybullet==3.2.6

# Alternative: If pybullet fails, use gymnasium's pybullet wrapper
# gymnasium[mujoco]==1.0.0  # MuJoCo is more stable on M-series

# Hyperparameter Optimization
optuna==4.1.0

# Visualization & Logging
tensorboard==2.18.0
matplotlib==3.9.3

# Configuration & Data
pyyaml==6.0.2
numpy==1.26.4
pandas==2.2.3

# Utilities
tqdm==4.67.1
opencv-python==4.10.0.84
pillow==11.0.0

# For pygame (helps with rendering)
pygame==2.6.1